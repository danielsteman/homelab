# direnv configuration for homelab
# Automatically sets environment variables when entering this directory
# https://direnv.net

# SOPS age key for secret encryption/decryption
export SOPS_AGE_KEY_FILE="$PWD/secrets/age-key.txt"
