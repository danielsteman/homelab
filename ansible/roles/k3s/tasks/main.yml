---
# Main tasks - routes to server or agent based on role

- name: Include server tasks
  ansible.builtin.include_tasks: server.yml
  when: k3s_role == 'server'

- name: Include agent tasks
  ansible.builtin.include_tasks: agent.yml
  when: k3s_role == 'agent'
