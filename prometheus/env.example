# Proxmox API Credentials for pve-exporter
# Copy this file to .env and fill in your values
# .env is gitignored for security

# Proxmox API URL (update IP address)
PVE_URL=https://192.168.1.100:8006/api2/json

# Option 1: API Token (Recommended)
# Create at: Proxmox UI → Datacenter → Permissions → API Tokens
# Token ID format: username@realm!token-name
PVE_TOKEN_ID=root@pam!prometheus
PVE_TOKEN_SECRET=your-api-token-secret-here

# Option 2: Username/Password (Less secure - only use if API token not available)
# PVE_USERNAME=root@pam
# PVE_PASSWORD=your-password-here
