# Proxmox API Credentials for pve-exporter
# Official exporter: https://github.com/prometheus-pve/prometheus-pve-exporter
# Copy this file to .env and fill in your values
# .env is gitignored for security

# Proxmox user (realm: pve for Proxmox users, pam for system users)
PVE_USER=prometheus@pve

# Option 1: API Token (Recommended)
# Create token: pveum user token add prometheus@pve prometheus --privsep 0
# Token name (the part after the ! in token ID)
PVE_TOKEN_NAME=prometheus
# Token secret (get from: pveum user token list prometheus@pve)
PVE_TOKEN_VALUE=your-api-token-secret-here

# Option 2: Username/Password (Less secure - only use if API token not available)
# PVE_PASSWORD=your-password-here

# SSL verification (set to false for self-signed certs)
PVE_VERIFY_SSL=false

# Configuration module name (default is fine)
PVE_MODULE=default
