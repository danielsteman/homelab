# Copy this file to terraform.tfvars and fill in your values
# terraform.tfvars is gitignored for security

proxmox_url          = "https://192.168.1.100:8006/api2/json"
proxmox_token_id     = "root@pam!terraform"
proxmox_token_secret = "your-api-token-secret"
proxmox_node         = "pve"

template_name = "nixos-template"
storage       = "local-lvm"
default_user  = "daniel"

# Note: SSH keys and network configuration are handled via NixOS configs
# See nix/hosts/ for node-specific configurations
